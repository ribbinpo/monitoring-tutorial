apiVersion: 1

providers:
  - name: "Grafana"
    orgId: 1
    folder: "Services"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10 # default is 10
    options:
      path: "/etc/grafana/provisioning/dashboards"
